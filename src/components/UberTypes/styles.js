import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  confirm: {
    backgroundColor: '#000',
    padding: 10,
    margin: 10,
    alignItems: 'center',
    
  },
  text: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 16,
  }
});

export default styles;
